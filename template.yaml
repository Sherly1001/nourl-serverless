AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31

Parameters:
  IsProd:
    Type: String
    Default: "true"
    AllowedValues:
      - "true"
      - "false"
  DomainName:
    Type: String
    Default: "nourl.space"
  CorsOrigin:
    Type: String
    Default: ""
  NotFoundFallbackUrl:
    Type: String
    Default: ""
  MongoUrlSsmPath:
    Type: String
    Default: "/nourl/mongo-url"

Conditions:
  IsProdCondition: !Equals [!Ref IsProd, "true"]

Globals:
  Function:
    Timeout: 30
    MemorySize: 512
    Runtime: nodejs22.x

Resources:
  HttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: $default

  NoUrlFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ./
      Handler: dist/lambda.handler
      Environment:
        Variables:
          MONGO_URL: !Sub "{{resolve:ssm:${MongoUrlSsmPath}}}"
          NOTFOUND_FALLBACK_URL: !Ref NotFoundFallbackUrl
          CORS_ORIGIN: !If
            - IsProdCondition
            - !Sub "https://${DomainName}"
            - !Ref CorsOrigin
      Events:
        RootGet:
          Type: HttpApi
          Properties:
            ApiId: !Ref HttpApi
            Path: /
            Method: GET
        ProxyAny:
          Type: HttpApi
          Properties:
            ApiId: !Ref HttpApi
            Path: /{proxy+}
            Method: ANY
    Metadata:
      BuildMethod: makefile

  Certificate:
    Type: AWS::CertificateManager::Certificate
    Condition: IsProdCondition
    Properties:
      DomainName: !Ref DomainName
      ValidationMethod: DNS

  ApiGatewayDomainName:
    Type: AWS::ApiGatewayV2::DomainName
    Condition: IsProdCondition
    Properties:
      DomainName: !Ref DomainName
      DomainNameConfigurations:
        - CertificateArn: !Ref Certificate
          EndpointType: REGIONAL

  ApiMapping:
    Type: AWS::ApiGatewayV2::ApiMapping
    Condition: IsProdCondition
    DependsOn: HttpApiApiGatewayDefaultStage
    Properties:
      ApiId: !Ref HttpApi
      DomainName: !Ref ApiGatewayDomainName
      Stage: $default

Outputs:
  ApiGatewayDomainTarget:
    Condition: IsProdCondition
    Description: "Add as Cloudflare CNAME target (orange cloud)"
    Value: !Sub "${ApiGatewayDomainName.RegionalDomainName} (CNAME target for ${DomainName})"
